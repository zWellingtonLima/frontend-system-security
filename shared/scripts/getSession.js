// CRIAR FETCH PARA CHAMAR O BACKEND BUSCANDO UMA SESSAO ATIVA DO USUARIO ATUAL
// A busca vai retornar true ou false e podemos usar na verificacao aqui embaixo

// // CASO TENHA, REDIRECIONA PARA DASHBOARD
// if (session && JSON.parse(session)) {
//   window.location.href = "app/dashboard.html";
// }
